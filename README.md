# Mini-Bank-System
Mini Bank System is a Python-based project that simulates a simple banking system with user accounts stored in a JSON file. It allows users to create accounts, log in, deposit and withdraw money, with each account having its own persistent balance saved locally.
